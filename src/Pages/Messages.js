import React from 'react';
import Header from '../Components/Header/Header';
const Messages = () => {
    return (
        <div>
            <Header/>
        </div>
    );
}

export default Messages;
