import React from 'react';
import Header from '../Components/Header/Header';

const Teachers = () => {
    return (
        <div>
            <Header/>
        </div>
    );
}

export default Teachers;
